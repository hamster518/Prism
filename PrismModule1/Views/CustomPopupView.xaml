<UserControl x:Class="PrismModule1.Views.CustomPopupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <StackPanel>
            <TextBlock Text="{Binding Notification.Title}" FontSize="24" HorizontalAlignment="Center" />
            <TextBlock Text="{Binding Notification.Content}" Margin="10"/>
        </StackPanel>
    </Grid>
</UserControl>
